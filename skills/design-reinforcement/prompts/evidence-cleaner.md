# Evidence cleaner contract

Run deterministic cleaning before any model call. Canonicalize source URLs, remove duplicate media URLs, cap each competitor at five evidence records, cap the run at twenty, and retain rejection reasons. Do not use an LLM to invent quality or performance labels.
